using namespace std;

#include<iostream>
#include<algorithm>
#include<vector>

int solution(vector<int> &A) {
  std::sort (A.begin(), A.end());

  int previous_element = A.front();
  for (auto const& element : A) {
    if
  }
}
